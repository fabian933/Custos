# Ed25519 private key (PKCS#8 DER, base64). Generate with: npm run genkey
CUSTOS_SIGNING_KEY=

# Optional. Without it /api/query falls back to the keyword parser.
OPENAI_API_KEY=
CUSTOS_LLM_MODEL=gpt-4o-mini
